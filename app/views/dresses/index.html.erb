<div class="container">

  <p id="notice"><%= notice %></p>

  <h1>Listing Dresses</h1>

  <table class="table">
    <thead>
      <tr>
        <th>Designer</th>
        <th>Location</th>
        <th>Main image</th>
        <th>Price</th>
        <th>Style</th>
        <th>Garment type</th>
        <th>Tag</th>
        <th>Name</th>
        <th>Collection</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @dresses.each do |dress| %>
        <tr>
          <td><%= dress.designer_id %></td>
          <td><%= dress.location %></td>
          <td><%= image_tag(dress.main_image.url, :class => "img-thumbnail") %></td>
          <td><%= dress.price %></td>
          <td><%= dress.style %></td>
          <td><%= dress.garment_type %></td>
          <td><%= dress.tag %></td>
          <td><%= dress.name %></td>
          <td><%= dress.collection %></td>
          <td><%= link_to 'Show', dress %></td>
          <td><%= link_to 'Edit', edit_dress_path(dress) %></td>
          <td><%= link_to 'Destroy', dress, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= link_to 'New Dress', new_dress_path, class: 'btn btn-default' %>
</div>